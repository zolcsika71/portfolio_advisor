## Graphify relationship handling

- `EXTRACTED` edges may be used as source-backed methodology context.
- `INFERRED` edges may only be used for discovery and navigation.
- Codex must not convert inferred edges into executable financial rules unless
  the rule also appears in a reviewed YAML or Markdown specification.
- When multiple start nodes are returned, Codex should ignore unrelated
  communities and narrow the query. 